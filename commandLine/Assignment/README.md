# CLI  Assignment #

```
Write ONE shell script to do following:
1.  create a directory .Enter it. create pets.txt  then write "cat" to pets.txt
   Then append the text "dog" to pets.txt
   Then append the text "hamster" to pets.txt
2. Then create commands.txt then  write the text "cat" to commands.txt
   Then append the text "ls" to commands.txt
   Then append the text "pwd" to commands.txt
3. Then find unique strings from these two files pets.txt and commands.txt
   and store the unique strings in lovelyCommands.txt
4. create a  new directory. Enter a directory. Create an empty file named greetings.txt 
5. Then write the content "Hello" five times to the file greetings.txt. 
6. Then create 5 flies named 1.txt, 2.txt, 3.txt, 4.txt and 5.txt and  write  the contents of  greetings.txt into each file  .
```
